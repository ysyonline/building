# 2026-09-23 工作日志

## git pull（首次失败后重试成功）
- `git pull` 首报 `Failed to connect to github.com:443 over proxy 127.0.0.1`；查明全局 `http.https://github.com.proxy=http://127.0.0.1:7892`，该端口由 **`bludcloudCore`** 进程监听且在运行，属代理偶发抖动。直连（`-c http.https://github.com.proxy=`）必 `Recv failure`。**重试即成功，未改任何 git 配置**（已写入 ~/.workbuddy/MEMORY.md）。
- 拉入 40 文件 / +14056 行：新增 `F3-data-tables.md`、`F4-deterministic-rng.md`、`GW-VS-EXT-01-playability-review.md`，垂直切片 vs2/vs3/vs4 全套 + `graybox-vs.html`。

## GW-VS-04 收口（团队 gw-vs-closeout）
项目处 **Phase 4 尾巴 / VS-4 收口**：灰盒跑通 VS-1→VS-4（40 PASS/0 FAIL），外部评审 EXT-01 已落地（核心结论：零结构性硬伤，真正缺第一个真人试玩；VS-6 前冻结数值）。

### 主理人裁定（A-G）
- **A→A′装填语义**：维持 C4.2 字面式。核到 `_vs4-app.js:1794` 与 `:2610` **两套互斥口径共存**，且 2610 从未运行（挂账 6 未销账）→ 判定「实现正确、断言非权威」，要求 GDD **显式记载双语义**而非粉饰，标 `[DISPUTED·VS-7]`。
- **B** OFFBOARD 目标集正式入 C8（含 BE-7 断言）。
- **C** 键名单向收干 camelCase；**F（新增）** units↔economy 跨表缝用**形态丙**：units 加 `costKey`，与**已在用的 `lootKey` 同构**（实证 `_vs4-app.js:659-663` 每个模板已带 lootKey）。
- **D→4 项勘误**：setupTurns / `weapons.melee.damage` / `aiScripts.congestionGravity` / **`units.*.speed` 五值**。
- **G（新增）** sheet §6.4/§6.5 加 `[口径注记]`（纸面按减半折算 vs 实现每轮 1 发 ⇒ W4 结论偏宽松，不得作 V1 基线）——性质＝测量工具效度声明，冻结令豁免。

### 关键事实（本人独立核验，勿信成员自述）
- 四处 GDD 改动 `git diff --stat` = **+39/-10，全在描述/标识符/测量层**，sheet §5 数值表与 §6 算式零触碰 → 零数值改动成立。
- **`units.*.speed` 不是「GDD 也没有」**：C2 §3.6:263 明确定义 `speed(u)` 并注「值 F3 宿主」；真缺＝**值宿主链断裂**（GDD 委托 F3 → VS 权威 sheet 无键 → 实现硬编码 `_vs4-app.js:659-663`，值 garrison 3 / ladder 3 / horse_archer 5 / warlord 4 / ram_chariot 2）。speed 是 F2.1 行动序首键 `(−speed, tier, unitId)`，五值四互异 ⇒ **correctness 风险**非整洁度。
- 两位成员**独立交叉命中**同一处错位（eng §2.4 观察-2 / design 异议 2）→ 纸面/实现口径不一致坐实。

### VS-5 质量门：CONCERNS（不自行开工）
- P0 **F-1** `F4State.logBytes` 不符 F4 §2.5 字节流，`snapshot()/restore()/checksumOf()` 三接口未实装 ⇒ checksum 对拍不成立。
- P0 **F-4** 浏览器 check 从未建立（正是 2610 潜伏分歧的成因）。
- P1/P2 F-2 事件缺 stats / F-3 `order_rejected` 未发出 / F-5 `C4.5 progress()` 未实装。

### 产出文件
- `design/spikes/vs4-tech-adjudication.md`（程基岩，五节）
- `design/reviews/GW-VS-04-consolidation.md`（文策渊，§1-§7）
- 修订：`C4-siege-phase.md` v1.0.3 / `C6-economy.md` v1.0.4 / `C8-xiongnu-ai.md` v1.0.4 / `vs-playtest-sheet.md` v1.0.2（仅 §C 锚点）
- 均未 git commit。

## GW-VS-04 收口（终态）
- 最终改动 **7 份 +78/-17**：C2-units v1.1.4 / C4 v1.0.3 / C6 v1.0.4 / C8 v1.0.4 / F3 v1.0.2 / X3 v1.1 / sheet v1.0.2；新增 `GW-VS-04-consolidation.md` 与 `vs4-tech-adjudication.md`；**零游戏数值改动**（三轮 diff 均逐行核验）。**均未 git commit**。
- 一次性清零的隐形债务（实现有/文档无）：`setupTurns`、`units.*.speed` 五值、`weapons.melee.damage`、`aiScripts.congestionGravity`、`lootKey`、`costKey`。
- 团队 gw-vs-closeout 已 shutdown + TeamDelete。待用户决定：N4（浏览器 check）与 VS-5 两道 P0 门是否立项。

## 归档与提交（2026-09-23 收工）
- 团队 gw-vs-closeout 已 shutdown + TeamDelete。
- 本批全部改动（7 份 GDD/评测单修订 + 2 份新增交付 + 本日志）**已提交并推送到 origin/main**。
- 遗留待用户决定：**N4（浏览器侧 `vs4-browser-check.js`）** 与 **VS-5 两道 P0 门（F-1 F4 三接口 / F-4）**——未立项，本批未做。下次开工首选项。

## 裁决界线（本轮沉淀，后续批次沿用）
> **补已知缺口 = 批准；美化未确认的分歧 = 拒绝。**
（据此：收编 `lootKey`、C2 补 costKey/lootKey = 前者，批准；K2 四份 GDD 分界注记 = 后者，拒绝本轮。）
「编排者核验」的三条硬做法：①不接成员自述，`git diff --stat` + 数值区 grep 自己跑；②两条成员独立发现交叉命中才算坐实；③成员报错先看是哪一层错了（本次 units.speed 的层次定性被我改过），别原样转发。

## GW-VS-N4 批次（团队 gw-vs-n4，用户批「N4 补建浏览器 check + 完成即提交推送」）
- **立项**：只做 N4（补建 `vs4-browser-check.js`），F3 注入/F-1/P1-P2 一律未批。用户选「本批修 c7，其余挂账」→ 最终扩到 N4-4/N4-5（二次授权，附覆盖保全条件）。
- **主理人 Phase-0 核验推翻成员估算**：consolidation 记「改 3 个方法名即可」偏乐观，实测 6 处差异——①桥名 `VS4Selftest`（旧桥 `VS3Selftest` 仍在 `:2551`，照抄会静默测旧面）②新增 economy/loot/c7/c4/dPhase 五口 ③签名 `hitRateCheck(n)` 必须传参 ④UI 新增 `btn-economy` ⑤c4 口含 DISPUTED 断言 ⑥out 文件名撞车风险。逐条写进任务卡。
- **⭐ 核心发现（N4 最大价值）**：无头 `vs4-check.js:459-464` 的排水循环有三重护栏（`spent<20*cost50` 预算上限 / `if(!r.ok)break` / 成功后 REDEPLOY→OFFBOARD 腾格），而浏览器侧 `graybox-vs.html:2664/:2667` **三重全无且不腾格** → 5_1_1 容量 2 已占 D_2，二次起恒 CELL_FULL 零扣费，余额冻结 430 → 双死循环。`⇒ 「40 PASS/0 FAIL」只证明无头路径健康，浏览器侧自测代码从未执行过，退化成死循环都无人知晓`。这是 N4 缺席 6 条挂账的真实代价。
- **四条挂账**：N4-1 c7 死循环（P0，点诊断按钮即卡死，阻塞 VS-6）→ 本批修；N4-2 `setup_not_ready` 断言前提不成立（初始床弩 ACTIVE 非 CONSTRUCTING）；N4-3 `drop_no_crew_or_success` 几何不可达（dropRadius=1）；N4-4 `deploy_cell_full` 应 OUT_OF_ZONE（对齐 vs4-check.js:408 + C7 §2.4 闸序）；N4-5 `bankrupt_insufficient` 目标格 7_1_1 被占，世界闸先于资金闸 ⇒ 恒不可达。N4-4/N4-5 本批修，N4-2/N4-3 挂 VS-7。
- **主理人补充裁定（成员建议里缺的一环）**：只把 `deploy_cell_full` 期望改 OUT_OF_ZONE 会让浏览器侧**彻底失去 CELL_FULL 覆盖** ⇒ 强制按无头版 `6_1_1` 连投三次补独立 CELL_FULL 断言。界线＝**禁止拿降低覆盖换绿灯**。成员另加自清理（防 6_1_1 占满卡死下游 `redeploy_move_free`）。
- **独立复核（本人跑，非采信自述）**：`git diff --stat` = 2 文件 +72/-14，且 `graybox-vs.html` 与 `_vs4-app.js` **同步 +43/-7**（产物由源生成，行数一致 ⇒ 无手改产物、无夹带）；数值区 grep 命中全为**读取**（`TABLES.economy.cost.*` 用于断言比对），damage/reloadTurns/range/standFast/speed 零命中 ⇒ **零数值改动成立**。build 两次 sha256 `0A246AF9…` 一致；vs4-check 40 PASS/0 FAIL；vs4-browser-check exit 0 + `judged: ALL PASS`；c7 `checks(22)` 含两条并存；c4 仍 ISOLATED-FAIL 三条未变。
- **反篡改校验机制（新沉淀）**：给成员派修复任务时，同时指定「修完后某处必须**仍然 FAIL 且失败项一字不变**」——若它变绿即证明动了禁区。比事后审 diff 更早拦住越界。
- **成员纪律亮点**：两次都在授权边界外停下上报（N4-1 界外两条、msedge 进程不敢误杀），未自行扩范围。
- **遗留**：N4-2/N4-3 挂 VS-7；F-1（F4 字节流三接口）、F-2/F-3/F-5、F3 全量注入、VS-5 门仍未销；VS-6 试玩未发生。
