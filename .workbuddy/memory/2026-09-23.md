# 2026-09-23 工作日志

## git pull（首次失败后重试成功）
- `git pull` 首报 `Failed to connect to github.com:443 over proxy 127.0.0.1`；查明全局 `http.https://github.com.proxy=http://127.0.0.1:7892`，该端口由 **`bludcloudCore`** 进程监听且在运行，属代理偶发抖动。直连（`-c http.https://github.com.proxy=`）必 `Recv failure`。**重试即成功，未改任何 git 配置**（已写入 ~/.workbuddy/MEMORY.md）。
- 拉入 40 文件 / +14056 行：新增 `F3-data-tables.md`、`F4-deterministic-rng.md`、`GW-VS-EXT-01-playability-review.md`，垂直切片 vs2/vs3/vs4 全套 + `graybox-vs.html`。

## GW-VS-04 收口（团队 gw-vs-closeout）
项目处 **Phase 4 尾巴 / VS-4 收口**：灰盒跑通 VS-1→VS-4（40 PASS/0 FAIL），外部评审 EXT-01 已落地（核心结论：零结构性硬伤，真正缺第一个真人试玩；VS-6 前冻结数值）。

### 主理人裁定（A-G）
- **A→A′装填语义**：维持 C4.2 字面式。核到 `_vs4-app.js:1794` 与 `:2610` **两套互斥口径共存**，且 2610 从未运行（挂账 6 未销账）→ 判定「实现正确、断言非权威」，要求 GDD **显式记载双语义**而非粉饰，标 `[DISPUTED·VS-7]`。
- **B** OFFBOARD 目标集正式入 C8（含 BE-7 断言）。
- **C** 键名单向收干 camelCase；**F（新增）** units↔economy 跨表缝用**形态丙**：units 加 `costKey`，与**已在用的 `lootKey` 同构**（实证 `_vs4-app.js:659-663` 每个模板已带 lootKey）。
- **D→4 项勘误**：setupTurns / `weapons.melee.damage` / `aiScripts.congestionGravity` / **`units.*.speed` 五值**。
- **G（新增）** sheet §6.4/§6.5 加 `[口径注记]`（纸面按减半折算 vs 实现每轮 1 发 ⇒ W4 结论偏宽松，不得作 V1 基线）——性质＝测量工具效度声明，冻结令豁免。

### 关键事实（本人独立核验，勿信成员自述）
- 四处 GDD 改动 `git diff --stat` = **+39/-10，全在描述/标识符/测量层**，sheet §5 数值表与 §6 算式零触碰 → 零数值改动成立。
- **`units.*.speed` 不是「GDD 也没有」**：C2 §3.6:263 明确定义 `speed(u)` 并注「值 F3 宿主」；真缺＝**值宿主链断裂**（GDD 委托 F3 → VS 权威 sheet 无键 → 实现硬编码 `_vs4-app.js:659-663`，值 garrison 3 / ladder 3 / horse_archer 5 / warlord 4 / ram_chariot 2）。speed 是 F2.1 行动序首键 `(−speed, tier, unitId)`，五值四互异 ⇒ **correctness 风险**非整洁度。
- 两位成员**独立交叉命中**同一处错位（eng §2.4 观察-2 / design 异议 2）→ 纸面/实现口径不一致坐实。

### VS-5 质量门：CONCERNS（不自行开工）
- P0 **F-1** `F4State.logBytes` 不符 F4 §2.5 字节流，`snapshot()/restore()/checksumOf()` 三接口未实装 ⇒ checksum 对拍不成立。
- P0 **F-4** 浏览器 check 从未建立（正是 2610 潜伏分歧的成因）。
- P1/P2 F-2 事件缺 stats / F-3 `order_rejected` 未发出 / F-5 `C4.5 progress()` 未实装。

### 产出文件
- `design/spikes/vs4-tech-adjudication.md`（程基岩，五节）
- `design/reviews/GW-VS-04-consolidation.md`（文策渊，§1-§7）
- 修订：`C4-siege-phase.md` v1.0.3 / `C6-economy.md` v1.0.4 / `C8-xiongnu-ai.md` v1.0.4 / `vs-playtest-sheet.md` v1.0.2（仅 §C 锚点）
- 均未 git commit。

## GW-VS-04 收口（终态）
- 最终改动 **7 份 +78/-17**：C2-units v1.1.4 / C4 v1.0.3 / C6 v1.0.4 / C8 v1.0.4 / F3 v1.0.2 / X3 v1.1 / sheet v1.0.2；新增 `GW-VS-04-consolidation.md` 与 `vs4-tech-adjudication.md`；**零游戏数值改动**（三轮 diff 均逐行核验）。**均未 git commit**。
- 一次性清零的隐形债务（实现有/文档无）：`setupTurns`、`units.*.speed` 五值、`weapons.melee.damage`、`aiScripts.congestionGravity`、`lootKey`、`costKey`。
- 团队 gw-vs-closeout 已 shutdown + TeamDelete。待用户决定：N4（浏览器 check）与 VS-5 两道 P0 门是否立项。

## 归档与提交（2026-09-23 收工）
- 团队 gw-vs-closeout 已 shutdown + TeamDelete。
- 本批全部改动（7 份 GDD/评测单修订 + 2 份新增交付 + 本日志）**已提交并推送到 origin/main**。
- 遗留待用户决定：**N4（浏览器侧 `vs4-browser-check.js`）** 与 **VS-5 两道 P0 门（F-1 F4 三接口 / F-4）**——未立项，本批未做。下次开工首选项。

## 裁决界线（本轮沉淀，后续批次沿用）
> **补已知缺口 = 批准；美化未确认的分歧 = 拒绝。**
（据此：收编 `lootKey`、C2 补 costKey/lootKey = 前者，批准；K2 四份 GDD 分界注记 = 后者，拒绝本轮。）
「编排者核验」的三条硬做法：①不接成员自述，`git diff --stat` + 数值区 grep 自己跑；②两条成员独立发现交叉命中才算坐实；③成员报错先看是哪一层错了（本次 units.speed 的层次定性被我改过），别原样转发。

## GW-VS-05 门批：F-1 销账（F4 规范字节流 + 三接口实装）
- **范围裁定**：用户批「开工」= 昨日日志遗留首选项 → 只做 F-1（vs4-tech-adjudication §5.4 P0 阻塞门）。F-2/F-3/F-5、K 系勘误、D 系裁定一律未动。
- **开工基线异象（已排除）**：首轮 `vs4-check.js` 出现一次 39 PASS/1 FAIL，随后 21 连跑全 40 PASS——**未能复现、未捕获失败项名**，疑似环境抖动而非套件非确定性（全套件走固定种子 mulberry32，无 Math.random 路径）。已记录存疑，未定案。
- **实装内容**（`_vs4-app.js` F4 分区 +130/-7）：
  - **S 规范字节流**（F4 §2.5）：init 建 header 14B（magic "F4L0G" 5B + logVer=1 1B + seedWord=state₀ 4B LE + entryCount 4B LE）；rand 每次追加 10B 定长条目（opTag 1B + domainTag=CORE 0x01 1B + cursorBefore/After 各 4B 显式小端）。**entryCount 须在 rand 内同步回写**（header 参与 checksum，S 必须随时自洽——初版漏写被自测抓出）。
  - **三接口**（F4 §3.2）：`checksumOf`=hex16(FNV1a64(S))；`snapshot()` 返回三字段拷贝；`restore(s,seed)` 先 INV-F4-1 链自洽/结构校验 + INV-F4-2 四角互证（step^cursor(state₀)=state），违者抛 `[F4 restore 拒载]` 前缀错误（fail-loud，零静默）。
  - **FNV-1a-64 双实现**（BE-2）：BigInt 参考版 + split-mul 等价版。**split-mul 踩坑两枚**：①素数 0x100000001B3 高 32 位=0x100（初版误写 0x01000000）；②h₀·p₀ 的 64 bit 积**高位段是真实进位来源**，列乘法必须完整进位传播，且归一禁用 `>>>0/<<`（ToUint32 会截断 >2³² 的列和）——须用 `%65536` 与 `Math.floor(t/65536)`。教训：**任何"显然等价"的位运算捷径都要先对 BigInt 参考实现跑 50 万随机互证再落**。
- **断言**：vs4-check.js 新增 VS5-F1.1~F1.7 七条（+111 行）：双实现互证（FNV 官方向量 foobar=85944171f73967e8 锚定 + 300 确定性伪随机串）/ S 形状 / 链自洽 F4.5 / checksum 敏感性 / snapshot-restore 往返（BE-6 打过的骰子不重摇）/ 损坏注入五路径全拒载 / 四角互证。**既有 40 条未动一字，VS4-R3 反向锚保持 PASS**。
- **踩坑**：断言夹具初版用 `new TextEncoder()`——vm sandbox 内该全局不存在（app 侧 utf8Bytes 有 ASCII 兜底所以侥幸同值，夹具不该赌）；改为纯 ASCII 手工编码。
- **验收证据**：①无头 47 PASS / 0 FAIL（21 回归+19 VS-4+7 VS5-F1），连跑 3 次稳定；②build-vs4 两次 sha256 `ecd2081d…` 幂等；③浏览器 `judged: ALL PASS`（14/14），c4 隔离区三失败项**原样不变**（反篡改锚成立：setup_not_ready / reload_derivation / drop_no_crew_or_success）；④零数值改动：diff 中 TABLES 表行/数值键零命中，改动全在 F4 分区与测试。
- **已知波动（不阻塞）**：浏览器 aiTiming 曾一次 FAIL（worst 12.8ms > budget 10ms，重跑 0.38-1.8ms 通过）——CDP 单页 eval 的调度抖动，非逻辑缺陷；另一次 msedge 启动崩溃 exit -1073741819，重跑即好。浏览器侧本就存在毫秒级噪声，budget=10ms 边界偏紧，VS-6 前可考虑放宽断言容差或多次取样（挂账，未批未动）。
- **未提交→已提交**：本批 4 文件（_vs4-app.js / graybox-vs.html / vs4-check.js / vs4-browser-out.txt）已 commit + push。
- **VS-5 剩余**：F-2（battle_won/lost 未带 stats，P1）、F-3（order_rejected 未发出，P1）、F-5（progress() 未实装，P2）——均为非阻塞；F-1 销账后 VS-5 存档批两道 P0 门（F-1/F-4）已全清。

## Phase 0 阶段诊断（20:3x，用户问「当工作室推进，现在走哪个阶段」）
- 主理人独立复核：git log 头部=d9367f0（gitignore 去重）、树干净、远端同步；昨日日志遗留的 N4/F-4 已由 VS-05 门批销账（vs4-browser-check 已建，14/14 ALL PASS）。
- **判定：Phase 5 制作期 · 垂直切片内部，VS-5 未完全收官**。已完成：VS-0~VS-4 + VS-5 的 F-1/F-4 两道 P0。剩余：VS-5 非阻塞挂账 F-2/F-3/F-5（P1/P1/P2）→ VS-6 真人试玩交付（EXT-01 评审认定的真正缺口）→ VS-7 反馈标定+GDD 销账。
- 给用户出了 3 选项：①收尾 VS-5 挂账（保守，约半批）②直接 VS-6 试玩（主线，外部评审推荐）③ F-2/F-3/F-5 与 VS-6 并行。待用户拍板，未 spawn 任何成员。

## GW-VS-05 尾批：F-2/F-3/F-5 销账（20:5x 用户拍板「先收 VS-5 尾巴」）
- **范围裁定**：三任务 #1 F-2 / #2 F-3 / #3 F-5 + #4 门批验收（依赖 1-3）。授权口径：F-2 走方案①（payload 带 stats，对齐 GDD，不升 battle_end_report 为正式契约）；F-3 二选一授权程基岩按代码事实判断，无天然触发面则降级挂账不造人工路径；F-5 按 §2.2-C-4 字面式 −1 分母实装纯函数（数据源先于表现层）。
- **硬约束下达**：既有 47 断言一字不动；c4 隔离区三失败项原样（反篡改锚）；零数值改动；产物幂等；新断言每销账≥1 条（F-2≥2）；**成员不自 commit**，验收证据齐回传主理人复核后统一提交。
- 已建团队 gw-vs5-tail，spawn engineering-lead（程基岩）执行中。等待回传。
